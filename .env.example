# Copy to .env and fill in. .env is gitignored — never commit real keys.
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: override the two models the eval compares (comma-separated OpenRouter IDs).
# Defaults are set in src/eval.py.
# NER_MODELS=google/gemini-2.0-flash-001,anthropic/claude-3.5-sonnet
